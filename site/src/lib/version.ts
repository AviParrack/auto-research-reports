// Site/engine version. Bump on substantive engine or schema changes.
// Distinct from per-report meta.yaml.current_pass (which counts passes
// run against that specific report) and from BUILD_HASH (which changes
// every build for cache-busting).
export const SITE_VERSION = 'v0.1.0';
export const SITE_STAGE = 'BETA';
